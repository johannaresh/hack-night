# CPU-only torch, so a cloud build doesn't pull the ~2GB CUDA build.
# Harmless locally (Windows/macOS already resolve the CPU wheel).
--extra-index-url https://download.pytorch.org/whl/cpu
torch

numpy
pyyaml
gymnasium
stable-baselines3

# Web app (server.py + web/) — FastAPI backend, static canvas frontend
fastapi
uvicorn[standard]

# RL workflow / desktop GUI (not needed by the web app, kept for local dev)
tensorboard
pytest
dearpygui
rerun-sdk
