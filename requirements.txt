# CPU-only torch, so Streamlit Cloud doesn't pull the ~2GB CUDA build.
# Harmless locally (Windows/macOS already resolve the CPU wheel).
--extra-index-url https://download.pytorch.org/whl/cpu
torch

numpy
pyyaml
gymnasium
stable-baselines3

# Web demo (streamlit_app.py)
streamlit
plotly

# RL workflow / desktop GUI (not needed by the web app, kept for local dev)
tensorboard
pytest
dearpygui
rerun-sdk
